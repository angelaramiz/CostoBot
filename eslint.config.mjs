// eslint-config-next v16 already exports a flat-config array natively.
// Node.js ESM can consume CJS modules via default import.
import nextConfig from 'eslint-config-next';

export default nextConfig;
