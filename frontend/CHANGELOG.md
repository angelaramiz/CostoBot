# Changelog — CostoBot

All notable changes are documented here.  
Format based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).  
Versioning follows [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [Unreleased]

### Added
- Project scaffolding (Next.js 14+ App Router + TypeScript strict)
- AUTONOMOUS versioning system (post-commit hook)
- Cascade calculation engine placeholder (Layer 1-4)
- IA adapter pattern (OpenRouter / LM Studio / Ollama)
- Firebase Auth integration scaffold
- MongoDB Atlas connection scaffold
- Zustand store with debounced backend sync

---

## [0.1.0] — Initial Setup

### Added
- AUTONOMOUS versioning system installed and configured
- `frontend/` directory with versioning scripts
- `backend/` directory with `/api/version` endpoint
- `.env.example` template with all required environment variables
- `.gitignore` with security-sensitive exclusions
- `CLAUDE.md` master rules for AI agents
- `PROJECT_CONTEXT.md` — stack documentation
- `ARCHITECTURE.md` — 8 ADRs
- `IMPLEMENTATION_ROADMAP.md` — 9-phase greenfield setup plan

---

*Se actualiza automáticamente con cada versión AUTONOMOUS.*
